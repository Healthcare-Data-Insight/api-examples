# ClaimInsight app with MongoDB
services:
  mongo:
    image: mongo:8
    restart: unless-stopped
    volumes:
      - ./mongo_data:/data/db
# Uncomment if you need to connect to MongoDB from your host
#    ports:
#      - "27017:27017"
  app:
    image: myarch/ediconvert:2.15.2
    restart: unless-stopped
    depends_on:
      - mongo
    volumes:
      - ./uploads:/app/uploads
      - ./load:/app/load
      - ./etc:/app/etc
    ports:
      - "5080:5080"