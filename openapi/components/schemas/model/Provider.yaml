---
allOf:
- $ref: "./Party.yaml"
- type: "object"
  properties:
    providerTaxonomy:
      description: "Provider's specialty information (taxonomy). Populated only for\
        \ billing, rendering, operating providers.<br/><a href='https://datainsight.health/code-lookup/?type=PROVIDER_TAXONOMY'\
        \ target='_blank'>View codes and descriptions</a>. EDI: PRV"
      $ref: "./Code.yaml"
