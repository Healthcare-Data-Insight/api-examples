---
type: "object"
description: "Segment: AMT."
properties:
  qualifierCode:
    description: "Code specifying the type of amount (amount qualifier code). EDI: AMT01."
    type: "string"
    x-edi-elt-designators:
    - "AMT01"
  type:
    description: "Type of amount; qualifier code translated to a mnemonic string constant (enum). EDI: AMT01."
    type: "string"
    x-edi-elt-designators:
    - "AMT01"
  amount:
    description: "Amount. EDI: AMT02."
    type: "number"
    format: "float"
    x-edi-elt-designators:
    - "AMT02"
required:
- "qualifierCode"
- "amount"
x-edi-seg-ids:
- "AMT"
