post:
  tags:
    - Generate EDI
  summary: Generate 835 EDI from JSON payments
  description: |-
    Generates X12 EDI `835` files from the request containing payments in JSON format.

  operationId: generate-edi-payment
  parameters:
    - name: failOnValidationErrors
      in: query
      description: >-
        Fail the request if validation errors are found. If not set, validation errors are logged but ignored.
      required: false
      schema:
        type: boolean
        default: false

    - name: fileName
      in: query
      description: >-
        The name of the EDI file submitted as text. This name is used in validation warnings and parsing errors. If not set, a randomly generated file name is used.
      required: false
      schema:
        type: string
  security: [ ]
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/model/EdiGenPaymentRequest.yaml
        examples:
          genRequest:
            summary: Request for 835
            externalValue: "https://raw.githubusercontent.com/Healthcare-Data-Insight/api-examples/refs/heads/release/2.15.0/edi_gen/request/835-minimal.json"


  responses:
    '200':
      description: |-
        Generated EDI file.
      content:
        text/plain:
          schema:
            description: >-
              EDI file content.
            type: string
          examples:
            generated835:
              summary: Generated 835 response
              externalValue: "https://raw.githubusercontent.com/Healthcare-Data-Insight/api-examples/refs/heads/release/2.15.0/edi_gen/response/835-minimal.edi"

    '400':
      description: EDI generation error or invalid parameter.
      content:
        application/json:
          schema:
            $ref: "../components/schemas/model/ErrorInfo.yaml"
    '417':
      description: Validation issues returned when `failOnValidationErrors` is set to `true` and validation issues are found.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/schemas/model/ValidationIssue.yaml"